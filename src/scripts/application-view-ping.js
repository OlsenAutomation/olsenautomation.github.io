// Compatibility entry point for the two preserved portfolio script tags.
// The shared module owns visit and resume alerts, so loading it twice is harmless.
import('/assets/visit-notifications.js');
